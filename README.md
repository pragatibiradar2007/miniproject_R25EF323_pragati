# mini project R25EF323_pragati

Small C project containing a single source file `mini.c` (the main program).

Build and run

Windows (with MinGW or similar):

```powershell
gcc -o mini.exe mini.c
.\mini.exe
```

Unix / WSL / Linux / macOS:

```bash
gcc -o mini mini.c
./mini
```

Files

- `mini.c` — main source file

License

Add a license file if you want to publish this repository publicly.
